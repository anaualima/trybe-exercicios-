import { X509Certificate } from "crypto"

type passaro = {
  asas: 2;
  bico: 1;
  bipede: true;
}

type sum = (x: number, y: number) => number;

type endereco = {
  rua: string;
  bairro: string;
  numero: number
  casa: true;
}